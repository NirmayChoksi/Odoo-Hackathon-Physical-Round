import { Component } from '@angular/core';

/** Route target for `/dashboard` index; `dashboardDefaultRedirectGuard` always navigates away first. */
@Component({
  standalone: true,
  template: '',
})
export class DashboardRedirectStubComponent {}
